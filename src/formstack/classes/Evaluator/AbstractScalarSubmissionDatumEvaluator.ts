import { AbstractEvaluator } from "./AbstractEvaluator";

abstract class AbstractScalarSubmissionDatumEvaluator extends AbstractEvaluator {
  //   abstract parseValues<S = string, T = string>(submissionDatum?: S): T;
  //   abstract isCorrectType<T>(submissionDatum: T): boolean;
}
export { AbstractScalarSubmissionDatumEvaluator };
