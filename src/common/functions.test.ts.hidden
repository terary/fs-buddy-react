import { TStatusRecord } from '../components/StatusMessageListContainer/type';
import { filterStatusMessages } from './functions';

describe('filterStatusMessages', () => {
  it('Should filter by text.', () => {
    // type TStatusRecord = {
    //   fieldId?: string | null;
    //   severity: TStatusMessageSeverity;
    //   message: string;
    //   relatedFieldIds?: string[] | null;
    // };

    const statusMessages: TStatusRecord[] = [
      {
        fieldId: 'field00001',
        severity: 'debug',
        message: 'Message One',
        relatedFieldIds: [],
      },
    ];
    expect(
      filterStatusMessages(statusMessages, ['debug'], 'One')
    ).toStrictEqual([]);
  });
});
